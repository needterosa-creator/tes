apiVersion: 1
datasources:
  - name: rce_ds
    type: yesoreyeram-infinity-datasource
    access: proxy
    url: http://execution:6060
    jsonData:
      allowedHosts:
        - execution
        - consensus
        - loki
        - prometheus
        - alloy
      httpAllowedMethods:
        - GET
        - POST
        - PUT
        - DELETE
